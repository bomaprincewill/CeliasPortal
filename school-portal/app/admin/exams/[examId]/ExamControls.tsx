"use client";
import { useState,useTransition } from "react";import {Loader2,Save} from "lucide-react";import {updateExamControls} from "@/actions/cbt/manageExam";import {Toast} from "@/components/ui";
export default function ExamControls({exam}:{exam:{id:string;title:string;durationMinutes:number;passMark:number;maxAttempts:number;isPublished:boolean;showResultImmediately:boolean}}){const[form,setForm]=useState(exam);const[toast,setToast]=useState<any>(null);const[pending,start]=useTransition();const save=()=>start(async()=>{const result=await updateExamControls({examId:exam.id,...form});setToast(result.success?{type:"success",message:"Exam settings saved."}:{type:"error",message:result.error??"Settings could not be saved."});});return <div className="card card-body space-y-4">{toast&&<Toast {...toast} onClose={()=>setToast(null)}/>}<h2 className="font-semibold">Exam controls</h2><div className="grid gap-3 sm:grid-cols-4"><div className="form-group sm:col-span-2"><label className="label">Title</label><input className="input" value={form.title} onChange={e=>setForm({...form,title:e.target.value})}/></div><div className="form-group"><label className="label">Duration (minutes)</label><input type="number" className="input" value={form.durationMinutes} onChange={e=>setForm({...form,durationMinutes:+e.target.value})}/></div><div className="form-group"><label className="label">Maximum attempts</label><input type="number" min="1" max="10" className="input" value={form.maxAttempts} onChange={e=>setForm({...form,maxAttempts:+e.target.value})}/></div><div className="form-group"><label className="label">Pass mark (%)</label><input type="number" className="input" value={form.passMark} onChange={e=>setForm({...form,passMark:+e.target.value})}/></div><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.isPublished} onChange={e=>setForm({...form,isPublished:e.target.checked})}/>Published</label><label className="flex items-center gap-2 text-sm"><input type="checkbox" checked={form.showResultImmediately} onChange={e=>setForm({...form,showResultImmediately:e.target.checked})}/>Release auto-graded results immediately</label></div><button disabled={pending} onClick={save} className="btn-primary self-start gap-2">{pending?<Loader2 className="h-4 w-4 animate-spin"/>:<Save className="h-4 w-4"/>}Save controls</button></div>}
